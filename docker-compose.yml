version: "3.3"

services:
  app:
    build: .
    ports:
      - "3002:3000"
    volumes:
      - .:/app
      - bundle:/usr/local/bundle
    command: bundle exec rails s -p 3000 -b '0.0.0.0'

volumes:
  bundle:
    driver: local
